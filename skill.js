/**
 * Evaluate tech skill execution
 */
let skill = {};
skill.evaluate = game => {
    try {
        let frames = game.getFrames(),
            ret = {},
            cache = [],
            hist = [];
        
        debugger;
    } catch (err) {
        console.error(err);
        debugger;
        return err;
    }
}